[package]
name = "lachat"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true

[dependencies]
minreq = "2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
libc = "0.2"
strsim = "0.11"
